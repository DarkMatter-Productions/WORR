{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "meson: setup",
            "type": "shell",
            "command": "cmd",
            "args": [
                "/c",
                "if exist builddir-win\\meson-private\\coredata.dat (meson setup --reconfigure builddir-win -Dbase-game=basew -Ddefault-game=basew) else (meson setup builddir-win -Dbase-game=basew -Ddefault-game=basew)"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
        {
            "label": "meson: compile",
            "type": "shell",
            "command": "meson",
            "args": [
                "compile",
                "-C",
                "builddir-win"
            ],
            "dependsOn": [
                "meson: setup"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
        {
            "label": "install: refresh (.install)",
            "type": "shell",
            "command": "python",
            "args": [
                "tools/refresh_install.py",
                "--build-dir",
                "builddir-win",
                "--install-dir",
                ".install",
                "--base-game",
                "basew"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        },
        {
            "label": "meson: build",
            "dependsOn": [
                "meson: compile",
                "install: refresh (.install)"
            ],
            "dependsOrder": "sequence",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
